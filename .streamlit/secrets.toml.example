# =============================================================================
# Modèle de configuration pour l'authentification Streamlit
# =============================================================================
# Instructions :
#   1. Copier ce fichier en  .streamlit/secrets.toml
#   2. Remplir les mots de passe hashés (bcrypt)
#   3. Ne JAMAIS committer secrets.toml (il est dans .gitignore)
#
# Générer un hash bcrypt :
#   python -c "import streamlit_authenticator as sa; print(sa.Hasher(['votre_mdp']).generate())"
# =============================================================================

[credentials.usernames.admin]
name     = "Administrateur"
password = "$2b$12$REMPLACER_PAR_HASH_BCRYPT"

# Ajouter d'autres utilisateurs si besoin :
# [credentials.usernames.biologiste]
# name     = "Biologiste"
# password = "$2b$12$REMPLACER_PAR_HASH_BCRYPT"

[cookie]
name        = "ecbu_auth"
key         = "REMPLACER_PAR_UNE_CLE_ALEATOIRE_LONGUE_ET_SECRETE"
expiry_days = 1
